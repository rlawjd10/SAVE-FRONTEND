<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    android:paddingBottom="20dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/item_add_iv"
        android:layout_width="306dp"
        android:layout_height="96dp"
        android:background="@drawable/fragment_child_red_off_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.495"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/item_add_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:src="@drawable/btn_add"
        app:layout_constraintEnd_toEndOf="@id/item_add_iv"
        app:layout_constraintStart_toStartOf="@id/item_add_iv"
        app:layout_constraintTop_toTopOf="@id/item_add_iv" />

</androidx.constraintlayout.widget.ConstraintLayout>